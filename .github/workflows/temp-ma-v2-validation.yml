name: Temporary M&A v2 validation

on:
  push:
    branches:
      - feature/acquisitions-holdings-v2

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: corepack enable
      - run: corepack prepare yarn@4.13.0 --activate
      - run: yarn install --immutable
      - run: yarn typecheck
      - run: yarn test:ci
